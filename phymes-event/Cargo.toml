[package]
name = "phymes-event"
version = "0.3.0"
edition = "2024"
authors = ["Douglas McCloskey"]
description = "The publish and subscribe events used in PHYMES"
license = "MIT AND Apache-2.0"
repository = "https://github.com/biom8er/phymes"
documentation = "https://docs.rs/phymes-event"

[features]
default = ["wsl"]
wasip2 = ["phymes-diagnostics/wasip2", "phymes-subject/wasip2", "phymes-schemas/wasip2"]
wsl = ["phymes-diagnostics/wsl", "phymes-subject/wsl", "phymes-schemas/wsl"]
hf_hub = []
api = ["phymes-subject/api"]
gpu = []
candle = []

[dependencies]
phymes-subject = { path = "../phymes-subject", default-features = false, optional = true }
phymes-diagnostics = { path = "../phymes-diagnostics", default-features = false, optional = true }
phymes-schemas = { path = "../phymes-schemas", default-features = false, optional = true }
arrow = { workspace = true, features = ["prettyprint"] }
anyhow = { workspace = true }
clap = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
