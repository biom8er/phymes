[package]
name = "phymes-processor"
version = "0.3.0"
edition = "2024"
authors = ["Douglas McCloskey"]
description = "The streaming message processor used in PHYMES"
license = "MIT AND Apache-2.0"
repository = "https://github.com/biom8er/phymes"
documentation = "https://docs.rs/phymes-processor"

[features]
default = ["wsl"]
api = ["phymes-core/api", "phymes-data/api", "phymes-message/api", "phymes-event/api", "phymes-streams/api"]
wasip2 = ["phymes-diagnostics/wasip2", "phymes-core/wasip2", "phymes-data/wasip2", "phymes-message/wasip2", "phymes-event/wasip2", "phymes-streams/wasip2", "phymes-schemas/wasip2", "tokio/sync", "tokio/macros", "tokio/io-util", "tokio/rt"]
wsl = ["phymes-diagnostics/wsl", "phymes-core/wsl", "phymes-data/wsl", "phymes-message/wsl", "phymes-event/wsl", "phymes-streams/wsl", "phymes-schemas/wsl", "tokio/full"]
hf_hub = ["phymes-streams/hf_hub"]
gpu = ["phymes-data/gpu", "phymes-streams/gpu"]
candle = ["phymes-streams/candle"] # force use of candle models when api is available

[dependencies]
phymes-core = { path = "../phymes-core", default-features = false, optional = true }
phymes-data = { path = "../phymes-data", default-features = false, optional = true }
phymes-schemas = { path = "../phymes-schemas", default-features = false, optional = true }
phymes-streams = { path = "../phymes-streams", default-features = false, optional = true }
phymes-diagnostics = { path = "../phymes-diagnostics", default-features = false, optional = true }
phymes-event = { path = "../phymes-event", default-features = false, optional = true }
phymes-message = { path = "../phymes-message", default-features = false, optional = true }
arrow = { version = "58.0.0", default-features = false, features = ["csv", "json", "ipc"] }
tokio = { version = "1.49.0", default-features = false, optional = true}
futures = { version = "0.3.31", default-features = false }
clap = { version = "4.2.4", features = ["derive"] }
anyhow = { version = "1", default-features = false }
parking_lot = "0.12.3"
tracing = "0.1.41"
serde_json = "1.0.133"
serde = "1.0.215"

[dev-dependencies]
criterion = { version = "0.8.2", default-features = false, features = ["cargo_bench_support"] }

[[bench]]
name = "candle_ops"
harness = false

[[bench]]
name = "chat"
harness = false