[package]
name = "phymes-core"
version = "0.2.2"
edition = "2024"
authors = ["Douglas McCloskey"]
description = "The core subject-based message passing algorithm of PHYMES"
license = "MIT AND Apache-2.0"
repository = "https://github.com/biom8er/phymes"
documentation = "https://docs.rs/phymes-core"

[features]
default = ["wsl"]
wasip2 = ["tokenizers/unstable_wasm",
    "tokio/sync",
    "tokio/macros",
    "tokio/io-util",
    "tokio/rt",
    "phymes-diagnostics/wasip2"]
wsl = ["dep:tempfile",
    "tokenizers/onig",
    "tokio/full",
    "phymes-diagnostics/wsl"]
hf_hub = [] # for compatibility with agents
openai_api = [] # for compatibility with agents
gpu = [] # for compatibility with agents
candle = [] # for compatibility with agents

[dependencies]
phymes-diagnostics = { path = "../phymes-diagnostics", default-features = false, optional = true }
arrow = { version = "56.2.0", default-features = false, features = ["csv", "json", "ipc", "prettyprint"] }
pin-project-lite = "0.2.16"
async-trait = "0.1.88"
anyhow = { version = "1", default-features = false }
clap = { version = "4.2.4", features = ["derive"] }
futures = { version = "0.3.31", default-features = false, features = ["alloc"] }
tokio = { version = "1.47.0", default-features = false, features = [], optional = true}
tracing = "0.1.41"
tracing-subscriber = "0.3.19"
tracing-chrome = "0.7.2"
serde = { version = "1.0.217", features = ["derive"] }
serde_json = "1.0.135"
tempfile = { version = "3.20.0", optional = true }
parking_lot = "0.12.3"
web-time = "1.1.0"
hashbrown = "0.15.4"
chrono = { version = "0.4.41", features = ["wasmbind"] }
minijinja = { version = "2.6.0", features = ["json"] }
minijinja-contrib = { version = "2.6.0", features = ["pycompat"] }
candle-core = { version = "0.9.1" }
tokenizers = { version = "0.22.1", default-features = false, optional = true }
bytes = "1.10.1"
getrandom = { version = "0.3.1", features = ["wasm_js"] }
num-traits = { version = "0.2", default-features = false }
