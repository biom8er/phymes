# Traces

Traces track the flow of subject messages through tasks and processors. Each subject message trace is composed of an `Entered` and `Exited` events that tracks the `message_name`, `subject_name`, and `tracer_type`.